#include "main.h"
#include <stdio.h>
/**
 * main - program that prints it's name
 * @argc: counter value
 * @argv: argument variable
 *
 * Return: Success
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
